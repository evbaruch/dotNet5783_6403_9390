<Window x:Class="PL.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:PL"
        mc:Ignorable="d"
        Title="MainWindow" Height="640" Width="800">
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="109*"/>
            <ColumnDefinition Width="291*"/>
        </Grid.ColumnDefinitions>

        <Grid.RowDefinitions>
            <RowDefinition Height="40*"/>
            <RowDefinition Height="500*"/>
            <RowDefinition Height="500*"/>
            <RowDefinition Height="350*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <Grid
            Name="UpGrid"
            Height="auto"
            Grid.Row="1"
            Width="auto" Grid.ColumnSpan="2">

            <Label 
            Content="Re-Evolution" 
            Width="auto"
            Height="80"
            FontSize="80"
            HorizontalAlignment="Center" 
            FontStyle ="Italic" 
            Foreground="DeepSkyBlue"
            FontFamily="Monotype Corsiva"/>

        </Grid>

        <Grid
            Name="MidGrid"
            Height="auto"
            Grid.Row="1"
            Width="auto" Grid.ColumnSpan="2" Margin="0,224,0,157" Grid.RowSpan="3">

            <Button 
            x:Name="AdminListWindow" 
            Content="god mode"  
            Width="100" 
            Height="30"
            Click="AdminMainWindow_Click"
            HorizontalAlignment="Stretch"
            FontWeight="Bold"
            >
                <Button.Resources>
                    <Style 
                    TargetType="Border">
                        <Setter 
                        Property="CornerRadius"
                        Value="5"/>
                    </Style>
                </Button.Resources>
            </Button>
            <Button Content="peasant mod" HorizontalAlignment="Center" Margin="0,133,0,0" VerticalAlignment="Top" Height="27" Width="100" Click="Button_Click"/>

        </Grid>
    </Grid>
</Window>
