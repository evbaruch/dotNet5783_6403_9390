using DO;
using System;
using System.Collections.Generic;
using System.ComponentModel.DataAnnotations;
using System.Linq;
using System.Text;
using System.Threading.Tasks;
using System.Xml.Schema;
using static DO.Enums;

namespace Dal;

partial class Program
{
    static void Main(string[] args)
    {
        Console.WriteLine("GG");



        //DalOrderItem b = new DalOrderItem();
        //for (int i = 0; i < 40; i++)
        //{
        //    Console.WriteLine(b.Read(i).ToString());
        //}

        //DalProduct c = new DalProduct();
        //c.Create("A", 1, productsCategory.healing, 10);
        //for (int i = 0; i < 11; i++)
        //{
        //    Console.WriteLine(c.Read(i).ToString());
        //}
    }

}
